const strings = ['hello', 'world', 'good']
const evenLength = strings.map((word)=>{
  if(word.length % 2 == 0){
    return word = true
  } else {
    return word = false
  }
})

console.log(evenLength)